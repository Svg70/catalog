import React from 'react'
import styles from './applicationAdmin.module.css'

const ApplicationAdmin = React.memo((props) => {
    
    return <div>Запросы на добавление в каталог</div>
  });

export default ApplicationAdmin